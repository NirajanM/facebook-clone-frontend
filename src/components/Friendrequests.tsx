import * as React from 'react';

interface IFriendrequestsProps {
}

const Friendrequests: React.FunctionComponent<IFriendrequestsProps> = (props) => {
    return (
        <>
            This is friend requests
        </>
    );
};

export default Friendrequests;
